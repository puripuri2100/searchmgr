export type config = {
  now_open_file_name: string | null,
}

export type data = {
  title: string;
  book_name: string;
  url: string;
  keywords: string[];
  time_stamp: string;
  memo: string;
}

