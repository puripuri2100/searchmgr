export type config = {
  now_open_file_name: string | null,
}

export type data = {
  title: string;
  book_name: string | null;
  url: string | null;
  keywords: string[];
  time_stamp: string | null;
  memo: string;
}

